# Assets

Shared, subject-independent visual material: the series mark, publisher marks,
and any image reused by several volumes.

The series mark is drawn in TikZ (`\dmzlogo` in `template/cover/docmath-cover.sty`),
so no bitmap logo is needed. Keep this directory free of raster images: figures
belong to `books/<volume>/figures/` and must be vector graphics.
